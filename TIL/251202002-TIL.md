## 오늘 배운 것

- git branch
- branching strategy
- collaboration
- git stash (아직 커밋하고 싶지 않은 변경 사항을 잠시 보관)
- git remote -v
- git remote add upstream {team repo URL}
- git fetch upstream main
 (내 로컬 저장소로 “upstream 저장소의 main 브랜치 최신 상태만 가져오는 명령)
- git merge FETCH_HEAD
- merge / rebase
- git push -u origin {branch_name}
  (브랜치를 처음 원격에 등록할 때 사용)

## 내일 할 일

- generate a new repository for python practice
 - git clone
- practice python basic skill 

## Issue

rebase -> merge ->  conflict에서 vim을 통해 conflict를 처리 후에도 conflict가

지속되는 경우에는 "git rebase --continue"를 사용

## 금지 커멘드

- git reset 
